# semtx - Tasks List

## Phase 1: Core CSS Foundation

1. Create CSS custom properties for colors (Latte light + Mocha dark), typography, spacing scale, borders, shadows
2. Implement minimal CSS reset (box-sizing, margins, typography base)
3. Build utility classes (display, flex, grid, spacing, sizing, responsive prefixes)
4. Create button components with all variants (primary, secondary, outline, ghost, danger, link) and sizes (sm, md, lg)
5. Build form input styles (input, textarea, select, checkbox, radio, file, switch) with states

## Phase 2: Components

6. Create card component (header, body, footer) with variants (default, flat, outline)
7. Build navigation components (navbar, sidebar, tabs, breadcrumb, pagination)
8. Create alert styles (info, success, warning, error) with dismissible feature using data-close
9. Build table styles (striped, hoverable, responsive wrapper, bordered)
10. Create modal styles (CSS-driven with data attributes, backdrop, header, body, footer, sizes)
11. Add skeleton (text, wave animation) and toast (auto-dismiss) components

## Phase 3: HTMX Integration

12. Add HTMX loading state classes (.htmx-indicator, .htmx-request, .htmx-requesting)
13. Implement swap transitions (.htmx-swapping, .htmx-settling with opacity fades)
14. Add active/navigate state styles (.htmx-history-navigate)

## Phase 4: Demo Page

15. Build index.html showcasing all components with live examples (buttons, forms, cards, nav, alerts, tables, modals, skeletons)
16. Add theme toggle (light/dark switch) and verify responsiveness across breakpoints

## Phase 5: Polish

17. Verify reduced motion support and accessibility (focus states, contrast, touch targets)
18. Final code review, minify CSS, update README with usage documentation